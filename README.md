# Painel Darvig Molas

Painel React/Vite integrado ao Supabase para login, dashboard e controle de estoque.

## Rodar localmente

```bash
npm install
npm run dev
```

## Build local

```bash
npm run build
```

Para abrir sem servidor, use `dist/index.html` depois do build.
